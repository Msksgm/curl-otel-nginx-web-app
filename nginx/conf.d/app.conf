upstream app_backend {
  server app:8080;
  keepalive 16;
}

server {
  listen 80;
  server_name _;

  # Serve static files directly via Nginx (optional)
  location /assets/ {
    alias /usr/share/nginx/html/;
    autoindex off;
    expires 1h;
  }
}
